tasks:
  - init: python3 -m venv venv && . venv/bin/activate && pip install requirements.txt
    command: echo "Replace me with something that should run on every start, or just
      remove me entirely."
image:
  file: .gitpod.Dockerfile
